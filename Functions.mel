proc NumberCounterThing(int $min, int $max, int $iterationAmount)
{
    for( $min=1; $max<101; $min = $min + $iterationAmount)
    {
        print($i + "\r\n");
    }
}

NumberCounterThing(10, 30, 1);